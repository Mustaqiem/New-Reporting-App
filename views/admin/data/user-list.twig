{% extends 'admin/template/app.twig' %}
{% block title %} Admin - User{% endblock %}
{% block current %}
    User list
{% endblock %}
{% block content %}
<div class="panel panel-primary">
    <div class="panel-heading main-color-bg">
        <h3 class="panel-title">User list</h3>
    </div>
    <div class="panel-body">
    <div class="col-xs-12">
    <a href=""><button class="btn btn-primary" style="border-radius:10%"><i class="fa fa-user-plus"></i> Create User</button></a>
      <div class="table-responsive">
    <br>
        <table  class="table table-bordered table-hover">
          <thead>
            <tr>
              <th>No</th>
              <th>Nama</th>
              <th>Jenis Kelamin</th>
              <th>Email</th>
              <th>No telepon</th>
              <th>Alamat</th>
              <th class="col-md-1">Option</th>
            </tr>
          </thead>
          <tbody>
            {% for user in data %}    
            <tr>
                <td> {{ loop.index }}</td>
                <td> {{ user.name }} </td>
                <td> {{ user.gender }} </td>
                <td> {{ user.email }} </td>
                <td> {{ user.phone }} </td>
                <td> {{ user.address }} </td>
                <td> 
                <a href="" style="color:red"  data-toggle="tooltip" data-placement="top" title="delete"><i class="fa fa-times"></i></a> 
                <a href="" style="color:#179FFC" data-toggle="tooltip" data-placement="top" title="Update"><i class="fa fa-pencil"></i></a>
                <a href="" style="color:green" data-toggle="tooltip" data-placement="top" title="View detail"><i class="fa fa-eye"></i></a>
                </td>
            </tr>
          {% endfor %}
          </tbody>
        </table>
            <nav aria-label="...">
                        <ul class="pager">
                            {% if pagination.current > pagination.first_page %}
                                <li class="previous"><a href="{{ base_url() }}/admin/user?page={{ pagination.first_page }}">
                                    <span aria-hidden="true">&larr;</span> Halaman pertama</a></li>
                                <li class="previous"><a href="{{ base_url() }}/admin/user?page={{ pagination.current - 1 }}">
                                    <span aria-hidden="true">&larr;</span> Kembali</a></li>
                            {% endif %}
                            {% if pagination.current < pagination.total_page %}
                                <li class="next"><a href="{{ base_url() }}/admin/user?page={{ pagination.total_page }}">
                                    Halaman terakhir <span aria-hidden="true">&rarr;</span></a></li>
                                <li class="next"><a href="{{ base_url() }}/admin/user?page={{ pagination.current + 1 }}">
                                    Selanjutnya <span aria-hidden="true">&rarr;</span></a></li>
                            {% endif %}
                          </ul>
                </nav>
                </td>
      </div><!--end of .table-responsive-->
    </div>
  </div>
</div>

    </div>
</div>
{% endblock %}